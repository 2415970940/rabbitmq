exchanges
On one side it receives messages from producers and
the other side it pushes them to queues
There are a few exchange types available: direct, topic, headers and fanout


exchange_declare->queue_declare->queue-bind
